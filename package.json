{
  "devDependencies": {
    "vitest": "^0.30.1"
  },
  "scripts": {
    "test": "vitest",
    "tcr": "vitest run && git add . && git commit || git checkout . && git clean -df && echo \"Ohhh, failed, reseting\"",
    "coverage": "vitest run --coverage"
  }
}
